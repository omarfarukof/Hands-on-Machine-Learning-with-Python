[project]
name = "hands-on-machine-learning-with-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "tensorflow[and-cuda]",
    "scipy",
    "matplotlib",
    "plotly",
    "ipykernel",
    "pandas",
    "seaborn",
    "scikit-learn",
    "ucimlrepo",
    "xgboost",
    "marimo>=0.11.2",
    "watchdog>=6.0.0",
    "imblearn>=0.0",
]

# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"

[tool.rye]
dev-dependencies = [
    "pandoc>=2.4",
    "nbconvert>=7.16.4",
]
